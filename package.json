{
  "devDependencies": {
    "parcel-bundler": "^1.12.5",
    "sass": "^1.42.1"
  },
  "scripts": {
    "start": "parcel src/index.html --no-source-maps --open",
    "build": "parcel build src/index.html --no-source-maps --out-dir public/ && cd public && postcss *.css -u autoprefixer -r --no-map && cd.."
  }
}
