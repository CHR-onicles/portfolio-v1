{
  "devDependencies": {
    "autoprefixer": "^10.4.9",
    "parcel-bundler": "^1.12.5",
    "postcss": "^8.4.16",
    "postcss-cli": "^10.0.0",
    "sass": "^1.54.9"
  },
  "scripts": {
    "start": "parcel index.html --no-source-maps --open -p 3000",
    "build": "parcel build index.html --no-source-maps --out-dir public/ && cd public && postcss *.css -u autoprefixer -r --no-map && cd.."
  }
}
